# talk-web 快速上手

## 5 分钟快速启动

```bash
# 1. 进入项目目录
cd /home/albert/talk-web

# 2. 查看当前状态
./status.sh

# 3. 测试 API（如果服务已启动）
./test-api.sh

# 4. 访问前端
# 浏览器打开: http://localhost:5173
# 登录账号: admin / admin123
```

## 当前状态 ✅

所有服务已启动并运行中：

- ✅ PostgreSQL (localhost:5432)
- ✅ Redis (localhost:6380)
- ✅ Go 后端 (http://localhost:8080)
- ✅ React 前端 (http://localhost:5173)

## 核心功能测试

### 1. 登录系统
1. 访问 http://localhost:5173
2. 输入账号: `admin`
3. 输入密码: `admin123`
4. 点击登录

### 2. 测试录音
1. 登录后进入主页
2. 按住录音按钮说话
3. 松开按钮自动上传
4. 查看上传结果

### 3. 用户管理（管理员）
1. 点击"管理后台"按钮
2. 可以创建、编辑、删除用户
3. 可以设置管理员权限

## API 测试

### 使用 curl 测试

```bash
# 健康检查
curl http://localhost:8080/health

# 登录获取 token
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# 获取用户列表（需要 token）
TOKEN="your-token-here"
curl http://localhost:8080/api/admin/users \
  -H "Authorization: Bearer $TOKEN"
```

### 使用测试脚本

```bash
./test-api.sh
```

## 日常操作

### 启动服务
```bash
./start.sh
```

### 停止服务
```bash
./stop.sh
```

### 查看状态
```bash
./status.sh
```

### 查看日志
```bash
# 后端日志
tail -f logs/server.log

# 前端日志
tail -f logs/web.log

# 数据库日志
docker-compose logs -f
```

## 配置说明

### 修改 talk-server 地址

编辑 `.env` 文件：

```bash
TALK_SERVER_URL=http://your-talk-server:5000
```

重启后端服务生效。

### 修改端口

**后端端口**（默认 8080）：
```bash
# .env
PORT=8080
```

**前端端口**（默认 5173）：
```bash
# web/vite.config.ts
server: {
  port: 5173,
}
```

## 故障排查

### 服务未启动
```bash
# 查看状态
./status.sh

# 启动服务
./start.sh
```

### 端口被占用
```bash
# 查看端口占用
lsof -i :8080
lsof -i :5173

# 停止服务后重启
./stop.sh
./start.sh
```

### 数据库连接失败
```bash
# 检查数据库
docker-compose ps

# 重启数据库
docker-compose restart postgres
```

## 下一步

- 📖 阅读完整文档: `README.md`
- 🚀 部署指南: `DEPLOY.md`
- 📋 项目计划: `PLAN.md`

## 常见问题

**Q: 麦克风权限被拒绝？**
A: 浏览器需要授权麦克风权限。如果是 HTTPS 网站，确保证书有效。

**Q: 音频上传失败？**
A: 检查 `TALK_SERVER_URL` 配置是否正确，确认 talk-server 服务是否运行。

**Q: 忘记管理员密码？**
A: 删除数据库重新初始化，或直接修改数据库中的密码哈希。

**Q: 如何创建新用户？**
A: 使用管理员账号登录，进入"管理后台"创建。

## 技术支持

- Issues: 提交到项目 issue tracker
- 文档: 查看 `README.md` 和 `DEPLOY.md`
